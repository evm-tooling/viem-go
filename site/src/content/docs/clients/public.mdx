---
title: Public Client
description: Read-only access to the Ethereum blockchain through standard JSON-RPC methods
---

import { CodeGroup } from '../../../components/CodePanel';

A Public Client provides read-only access to the Ethereum blockchain through standard JSON-RPC methods like `eth_blockNumber`, `eth_getBalance`, `eth_call`, and more.

## Creating a Public Client

<CodeGroup 
  client:load
  tabs={[
    {
      title: "Go",
      language: "go",
      code: `import (
    "github.com/ChefBingbong/viem-go/client"
    "github.com/ChefBingbong/viem-go/client/transport"
    "github.com/ChefBingbong/viem-go/chain/definitions"
)

publicClient, err := client.CreatePublicClient(client.PublicClientConfig{
    Chain:     definitions.Mainnet,
    Transport: transport.HTTP("https://eth.llamarpc.com"),
})
if err != nil {
    log.Fatal(err)
}
defer publicClient.Close()`
    },
    {
      title: "viem",
      language: "ts",
      code: `import { createPublicClient, http } from 'viem'
import { mainnet } from 'viem/chains'

const publicClient = createPublicClient({
  chain: mainnet,
  transport: http(),
})`
    }
  ]}
/>

## Configuration Options

### PublicClientConfig

| Parameter | Type | Description |
|-----------|------|-------------|
| `Chain` | `*chain.Chain` | Chain configuration (optional) |
| `Transport` | `TransportFactory` | Transport to use for RPC calls |
| `Key` | `string` | Client identifier (default: "public") |
| `Name` | `string` | Client name (default: "Public Client") |
| `PollingInterval` | `time.Duration` | Polling frequency for subscriptions |
| `CacheTime` | `time.Duration` | Cache duration for repeated calls |
| `ExperimentalBlockTag` | `BlockTag` | Default block tag for calls |

## Available Methods

### Block Methods

<CodeGroup 
  client:load
  tabs={[
    {
      title: "GetBlockNumber",
      language: "go",
      code: `blockNumber, err := publicClient.GetBlockNumber(ctx)
// Returns: uint64`
    },
    {
      title: "GetBlock",
      language: "go",
      code: `block, err := publicClient.GetBlock(ctx, client.BlockTagLatest, false)
// Returns: *types.Block`
    },
    {
      title: "GetBlockByNumber",
      language: "go",
      code: `block, err := publicClient.GetBlockByNumber(ctx, 12345678, true) // with transactions
// Returns: *types.Block`
    },
    {
      title: "GetBlockByHash",
      language: "go",
      code: `block, err := publicClient.GetBlockByHash(ctx, hash, false)
// Returns: *types.Block`
    }
  ]}
/>

### Account Methods

<CodeGroup 
  client:load
  tabs={[
    {
      title: "GetBalance",
      language: "go",
      code: `balance, err := publicClient.GetBalance(ctx, address)
// Returns: *big.Int (wei)`
    },
    {
      title: "GetTransactionCount",
      language: "go",
      code: `nonce, err := publicClient.GetTransactionCount(ctx, address)
// Returns: uint64`
    },
    {
      title: "GetCode",
      language: "go",
      code: `code, err := publicClient.GetCode(ctx, contractAddress)
// Returns: []byte`
    },
    {
      title: "GetStorageAt",
      language: "go",
      code: `value, err := publicClient.GetStorageAt(ctx, address, slot)
// Returns: []byte`
    }
  ]}
/>

### Transaction Methods

<CodeGroup 
  client:load
  tabs={[
    {
      title: "GetTransaction",
      language: "go",
      code: `tx, err := publicClient.GetTransaction(ctx, txHash)
// Returns: json.RawMessage`
    },
    {
      title: "GetTransactionReceipt",
      language: "go",
      code: `receipt, err := publicClient.GetTransactionReceipt(ctx, txHash)
// Returns: *types.Receipt`
    },
    {
      title: "WaitForTransactionReceipt",
      language: "go",
      code: `receipt, err := publicClient.WaitForTransactionReceipt(ctx, txHash)
// Polls until transaction is mined`
    }
  ]}
/>

### Call Methods

<CodeGroup 
  client:load
  tabs={[
    {
      title: "Call",
      language: "go",
      code: `result, err := publicClient.Call(ctx, types.CallRequest{
    To:   contractAddress,
    Data: calldata,
})
// Returns: []byte`
    },
    {
      title: "EstimateGas",
      language: "go",
      code: `gas, err := publicClient.EstimateGas(ctx, types.CallRequest{
    From: sender,
    To:   recipient,
    Data: data,
})
// Returns: uint64`
    }
  ]}
/>

### Fee Methods

<CodeGroup 
  client:load
  tabs={[
    {
      title: "GetGasPrice",
      language: "go",
      code: `gasPrice, err := publicClient.GetGasPrice(ctx)
// Returns: *big.Int`
    },
    {
      title: "GetMaxPriorityFeePerGas",
      language: "go",
      code: `priorityFee, err := publicClient.GetMaxPriorityFeePerGas(ctx)
// Returns: *big.Int`
    },
    {
      title: "GetFeeHistory",
      language: "go",
      code: `history, err := publicClient.GetFeeHistory(ctx, 10, client.BlockTagLatest, []float64{25, 50, 75})
// Returns: json.RawMessage`
    }
  ]}
/>

### Chain Methods

<CodeGroup 
  client:load
  tabs={[
    {
      title: "GetChainID",
      language: "go",
      code: `chainID, err := publicClient.GetChainID(ctx)
// Returns: uint64`
    },
    {
      title: "GetLogs",
      language: "go",
      code: `logs, err := publicClient.GetLogs(ctx, types.FilterQuery{
    FromBlock: big.NewInt(12345678),
    ToBlock:   big.NewInt(12345778),
    Addresses: []common.Address{contractAddress},
    Topics:    [][]common.Hash{{eventTopic}},
})
// Returns: []types.Log`
    }
  ]}
/>

## Contract Interactions

The Public Client also supports contract read operations:

<CodeGroup 
  client:load
  tabs={[
    {
      title: "Go",
      language: "go",
      code: `result, err := publicClient.ReadContract(ctx, client.ReadContractOptions{
    Address:      common.HexToAddress("0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48"),
    ABI:          \`[{"name":"balanceOf","type":"function","inputs":[{"name":"owner","type":"address"}],"outputs":[{"type":"uint256"}]}]\`,
    FunctionName: "balanceOf",
    Args:         []any{ownerAddress},
})

balance := result.(*big.Int)`
    },
    {
      title: "viem",
      language: "ts",
      code: `const balance = await publicClient.readContract({
  address: '0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48',
  abi: parseAbi(['function balanceOf(address) view returns (uint256)']),
  functionName: 'balanceOf',
  args: [ownerAddress],
})`
    }
  ]}
/>

See [Reading Contracts](/docs/contract/read-contract/) for more details.

## Block Tags

When querying state, you can specify which block to read from:

```go
const (
    BlockTagLatest    = "latest"    // Most recent mined block
    BlockTagPending   = "pending"   // Pending state
    BlockTagEarliest  = "earliest"  // Genesis block
    BlockTagSafe      = "safe"      // Safe head
    BlockTagFinalized = "finalized" // Finalized block
)

// Usage
balance, err := publicClient.GetBalance(ctx, address, client.BlockTagSafe)
```

## Error Handling

All methods return Go-style errors. RPC errors include additional context:

```go
balance, err := publicClient.GetBalance(ctx, address)
if err != nil {
    // Check if it's an RPC error
    var rpcErr *transport.RPCRequestError
    if errors.As(err, &rpcErr) {
        fmt.Printf("RPC Error %d: %s\n", rpcErr.Code, rpcErr.Message)
    }
    return err
}
```
